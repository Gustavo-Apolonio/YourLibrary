{
  "name": "your-library",
  "version": "1.0.0",
  "scripts": {
    "install:app": "bash install.sh",
    "build": "bash build.sh",
    "install-build": "bash install.sh build",
    "start": "concurrently \"npm run start --prefix backend\"",
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run start --prefix frontend\""
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}